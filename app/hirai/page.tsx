import HiraiAdmin from "@/components/HiraiAdmin";

export const metadata = {
  title: "Hirai Admin",
  robots: {
    index: false,
    follow: false,
  },
};

export default function HiraiPage() {
  return <HiraiAdmin />;
}
